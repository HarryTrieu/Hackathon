# Feature how-it-works docs

One file per meaningful feature (e.g. `analyze-api.md`, `home-upload.md`).

Required sections: Purpose · User workflow · Files and responsibilities · Data flow and dependencies · How to test · Out of scope.

Keep short. No source dumps. No secrets. Worklog stays at `/Worklog/` for changelog memory.
