# Feature docs

One markdown file per feature or vertical slice (e.g. `notes-api.md`, `home-ui.md`).

Agent creates these after an approved implementation. Keep each file short; use paths + line ranges, not full code paste.
